describe("Service Flow (Env + Report)", () => {
  before(() => {
    cy.visit("http://localhost:8002") // baseUrl 
  })

  it("Create Service with full URL", () => {
    cy.get('.cell-wrapper').eq(1).click()


 })
  })
  
